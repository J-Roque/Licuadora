length-> suma total dec elementos
[asas.length -1]
at(-2)-> nos permite el acceso desde inicio a fin reverso
Inmutabilidad ->cuando modificamos un valor
splace -> inserta elemento a medias en el array
conact ->union de arrays
indexOf('') ->retorana el indeice del elemento en caso que no este -1
slice(0, 3); elementos desde posision espesifica devielve
fill('pato',inicial,final)-> rellena de eloementos desde x rango a x

Operador spread ->
    - Copiar un array o objeto, Suma ...animales
    - Combinar arrays: ...animales, ...animales
    - Agregar elementos a un array: (...arrayOriginal, nuevoElemento)
sort() -> ordena elementos array